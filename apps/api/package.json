{
  "name": "@padel/api",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "lint": "biome check src package.json tsconfig.json tsconfig.build.json",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "@padel/schemas": "workspace:*"
  },
  "nx": {
    "tags": ["type:app", "scope:api", "platform:backend"]
  }
}
