# Forms and Validation

## Rules

- forms should use TanStack Form
- validation should use Zod schemas
- TanStack Form + Zod is the default form architecture for create, edit, and filter workflows
- field-level and submit-level validation behavior should be explicit
- shared validation rules should live in reusable schema boundaries where appropriate
