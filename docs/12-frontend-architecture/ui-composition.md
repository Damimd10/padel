# UI Composition

## Rules

- reusable UI should come from `packages/ui`
- app-local composition belongs in `apps/web`
- do not duplicate design-system primitives inside app features
- accessibility and component API decisions should align with the dedicated skills and design-system docs
