# Release and Versioning

## Purpose

Define how UI package changes participate in versioning and release planning.

## Rules

- reusable UI package changes should participate in Changesets when version impact exists
- changelog relevance should be considered when component APIs or behavior change
- release notes should reflect consumer-facing UI changes, not only internal refactors
